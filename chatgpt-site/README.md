# ChatGPT Sites deployment

This directory packages the canonical static specification site for ChatGPT
Sites. The mirror preserves the GitHub Pages URL as the canonical origin and
does not change normative artifact identifiers.

Run `npm run build` in this directory to rebuild `dist/` from the repository's
validated `site-dist/` bundle.
